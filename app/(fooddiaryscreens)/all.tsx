import React from 'react'
import { Text, View, StyleSheet } from 'react-native'

// TODO: Under the 'All' tab, make an API call to get the data for the nutritional values of the food items

const All = () => {
  return (
    <View style={styles.container}>
        <Text>Hello</Text>
    </View>
  )
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: "#E4FBFF"
    }
})

export default All;
